# Doo

## Tree Storage

DOO does not retain the expanded search tree by default. Set `store_tree=true` when debugging,
visualizing the search tree, or using tree utilities such as `get_tree_size` and
`get_all_leaves`.

```@autodocs
Modules = [NonArchimedeanMachineLearning]
Pages   = ["optimization/optimizers/tree_search/doo.jl"]
```
